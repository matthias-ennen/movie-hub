name: Deploy Firebase

on:
  push:
    branches: [main]
  schedule:
    # Daily refresh in the early morning. A failed refresh never reaches the
    # deploy step, so the last working catalog remains live.
    - cron: '17 03 * * *'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: firebase-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Generate TMDB catalog
        env:
          TMDB_API_READ_TOKEN: ${{ secrets.TMDB_API_READ_TOKEN }}
          TMDB_LANGUAGE: de-DE
        run: npm run tmdb:catalog

      - name: Build
        run: npm run build

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          project_id: movie-hub-62459
          workload_identity_provider: projects/612913221205/locations/global/workloadIdentityPools/github-actions/providers/github
          service_account: github-movie-hub-deploy@movie-hub-62459.iam.gserviceaccount.com

      - name: Deploy Hosting and Firestore Rules
        run: npx firebase-tools deploy --only hosting,firestore:rules --project movie-hub-62459 --non-interactive
